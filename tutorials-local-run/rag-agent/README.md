# RAG Agent

Build a Retrieval Augmented Generation (RAG) agent with LangChain.

## What It Covers

- Document retrieval pipeline
- Augmenting LLM context with retrieved content
- Agent loop over a knowledge base

## Framework

LangChain

## Prerequisites

- Python + API key
- No cluster or cloud needed — runs locally
